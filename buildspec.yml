version: 0.2

phases:
  pre_build:
    commands:
      - echo pre build running,repo is getting clonned from git 
  build:
    commands:
      - echo Build started on `date`
      - mvn clean package -DskipTests=true
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - target/aws-build-sample-maven-project-0.0.1-SNAPSHOT.jar